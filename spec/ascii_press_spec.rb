require 'spec_helper'

describe AsciiPress do
  it 'has a version number' do
    expect(AsciiPress::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
